name: CI

on:
  push:
    branches: ["**"]
    tags-ignore: ["**"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Ruff
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Installer ruff
        run: python -m pip install "ruff~=0.16.0"

      - name: ruff check
        run: ruff check --output-format=github .

      - name: ruff format --check
        run: ruff format --check --diff .

  tests:
    name: Pytest (Python 3.13, Linux)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: pyproject.toml

      # test_incident_followup_render.py exécute du JavaScript via node
      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Installer le projet et ses dépendances de développement
        run: python -m pip install -e ".[dev]"

      - name: pytest
        run: python -m pytest -q -ra

  commits:
    name: Messages de commit
    uses: cedric-HAOS/Ohana-Platform/.github/workflows/commit-messages.yml@main
